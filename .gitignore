node_modules
coverage
dist
.relay
.agent-harness
tui/target/
target/
gui/dist/
gui/src-tauri/target/
gui/src-tauri/gen/
gui/tsconfig.tsbuildinfo
# Mobile icon sets — only used if we target Android / iOS from Tauri. Generated
# on demand by `pnpm tauri icon`.
gui/src-tauri/icons/android/
gui/src-tauri/icons/ios/
# Local env files — never commit. The tracked template stays.
.env
.env.*
config.env
config.env.*
!config.env.template
